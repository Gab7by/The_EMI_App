import { View } from "react-native";

const Divider = () => (
    <View className="flex-row items-center gap-32">
        <View className="flex-1 h-[0.7px] bg-menorah-primary" />
        <View className="flex-1 h-[0.7px] bg-menorah-primary" />
    </View>
)

export default Divider